# Build artifacts
target/

# Editor
*.rs.bk
Cargo.lock.bak
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Temp
*.swp
*.swo
*~

# Stray root-level binary artifacts
# `test_struct` was a Mach-O binary accidentally committed at the
# repo root. It is not a project fixture; do not re-introduce it.
/test_struct

# Accidental terminal transcripts and generated logs.
# `typescript` is the default `script(1)` output file; prior test runs
# accidentally left one at the repo root. Test/corpus fixtures under
# tests/ and docs/ are legitimate source files and are not matched here.
/typescript
/typescript.*
*.script
script.log
coverage/
*.profraw
*.profdata

# opencode
.opencode/
